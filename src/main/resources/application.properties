spring.application.name=userservice

# PostgreSQL Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://34.100.194.154:5432/userService2}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:NorthstarDB2024}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Server Configuration
server.port=8082

# Email Service Configuration
email.service.url=${EMAIL_SERVICE_URL:http://localhost:8080}

# JWT Configuration
jwt.issuer.url=http://northstar-user:8082

# Logging - reduced for production
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=INFO